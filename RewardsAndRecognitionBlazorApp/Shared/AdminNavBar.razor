@using RewardsAndRecognitionBlazorApp.ViewModels
@inject NavigationManager Nav
@inject IJSRuntime JS
@inject UserSession Session

<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center" href="/dashboard/admin">
            <i class="bi bi-stars me-2"></i>
            <span>RewardsAndRecognition</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link @(ActivePage == "Dashboard" ? "active" : "")" href="/dashboard/admin">
                        <i class="bi bi-speedometer2 me-1"></i> Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(ActivePage == "Users" ? "active" : "")" href="/users">
                        <i class="bi bi-people-fill me-1"></i> Users
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(ActivePage == "Teams" ? "active" : "")" href="/teams">
                        <i class="bi bi-collection-fill me-1"></i> Teams
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(ActivePage == "YearQuarter" ? "active" : "")" href="/yearquarter">
                        <i class="bi bi-calendar3 me-1"></i> Year Quarter
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(ActivePage == "Category" ? "active" : "")" href="/categories">
                        <i class="bi bi-tags-fill me-1"></i> Category
                    </a>
                </li>
                <li class="nav-item">
                    <button class="btn btn-outline-light ms-2" @onclick="HandleLogout">
                        <i class="bi bi-box-arrow-right me-1"></i> Logout
                    </button>
                </li>
            </ul>
        </div>
    </div>
</nav>

@code {
    [Parameter]
    public string ActivePage { get; set; } = "";

    private async Task HandleLogout()
    {
        bool confirmed = await JS.InvokeAsync<bool>("confirmLogout");
        if (confirmed)
        {
            await Session.ClearSessionAsync(JS);
            Nav.NavigateTo("/", forceLoad: true);
        }
    }
}
